<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!-- تست‌ها برای چیدن سناریو (مثلاً دادن منابع به یک بازیکن) به setterهای internal نیاز دارند. -->
  <ItemGroup>
    <InternalsVisibleTo Include="Hexara.Domain.Tests" />
  </ItemGroup>

</Project>
